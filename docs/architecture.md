# Architecture

This application is composed of three initial services:

1. Next.js frontend
2. FastAPI backend
3. PostgreSQL database

The application is deployed using Docker Compose and is intended to run on a Rocky Linux-based home lab server.
